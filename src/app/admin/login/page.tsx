import type { Metadata } from "next";
import { LockKeyhole } from "lucide-react";
import { Container } from "@/components/ui";
import { loginAction } from "./actions";

export const metadata: Metadata = { title: "관리자 로그인", robots: { index: false, follow: false } };
const messages: Record<string, string> = { config: "Supabase 환경변수를 먼저 설정하세요.", invalid: "이메일 또는 비밀번호를 확인하세요.", unauthorized: "관리자 권한이 등록되지 않은 계정입니다." };
export default async function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const { error } = await searchParams; return <Container className="py-16"><div className="mx-auto max-w-md rounded-3xl border border-slate-200 bg-white dark:bg-zinc-900 p-7 shadow-lg"><div className="grid size-12 place-items-center rounded-2xl bg-slate-950 text-white"><LockKeyhole className="size-6" /></div><h1 className="mt-6 text-2xl font-black">관리자 로그인</h1><p className="mt-2 text-sm leading-6 text-slate-500">Supabase Auth 계정 중 admin_users에 등록된 사용자만 접근할 수 있습니다.</p>{error && <p className="mt-5 rounded-xl bg-rose-50 p-3 text-sm font-semibold text-rose-700">{messages[error] ?? "로그인할 수 없습니다."}</p>}<form action={loginAction} className="mt-6 space-y-4"><label className="block text-sm font-bold">이메일<input name="email" type="email" autoComplete="email" required className="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3 font-normal outline-none focus:border-emerald-500" /></label><label className="block text-sm font-bold">비밀번호<input name="password" type="password" autoComplete="current-password" required className="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3 font-normal outline-none focus:border-emerald-500" /></label><button className="w-full rounded-xl bg-slate-950 px-4 py-3 font-bold text-white hover:bg-emerald-700">로그인</button></form></div></Container>; }
